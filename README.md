# Password-generator
A Python-based password generator that creates secure random passwords based on the user’s preferred length. It uses uppercase and lowercase letters, numbers, and special characters, leveraging Python’s secrets module for cryptographically secure password generation.
